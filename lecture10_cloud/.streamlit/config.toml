# Streamlit settings that matter when running behind a PaaS reverse proxy.
[server]
headless = true              # never try to open a browser on the server
enableCORS = false           # TLS + routing are handled by the platform proxy
enableXsrfProtection = false # avoids websocket origin mismatches behind the proxy

[browser]
gatherUsageStats = false
